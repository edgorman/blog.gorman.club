// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.4
//   protoc               unknown
// source: blog/v1/user.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "blog.v1";

/**
 * User is a profile as anybody may read it - what `GET /users/{username}` answers with.
 *
 * It is the wire shape, not entity.User: the entity also carries SubscribedUntil, which a public
 * lookup must never disclose and which therefore has no field here at all. See `packages/protos/CLAUDE.md`'s
 * "Contract Layer" for why protos model the wire rather than the domain.
 */
export interface User {
  id: string;
  /**
   * The whole of a profile's public identity: both the handle it is looked up by and the name
   * readers see. There is deliberately no separate display name.
   */
  username: string;
  bio: string;
  createdAt?: string | undefined;
  updatedAt?: string | undefined;
}

/**
 * CurrentUser is the caller's own profile - what `GET /users/me` and `PUT /users/me` answer with.
 * It carries what this deployment lets the account do, and when its paid access runs out, neither
 * of which a lookup of somebody else's name ever discloses.
 *
 * The five profile fields are repeated here rather than nested under a `User` field, because the
 * wire is flat: Go reaches it by embedding entity.User and TypeScript by `extends User`, and proto3
 * can express neither. Nesting would be tidier to write and a breaking change to ship, so the
 * duplication is deliberate - renaming a profile field means editing both messages, and the
 * drift check is what catches forgetting to.
 */
export interface CurrentUser {
  id: string;
  username: string;
  bio: string;
  createdAt?: string | undefined;
  updatedAt?:
    | string
    | undefined;
  /**
   * Whether this account may use the AI writing assistant. The routes enforce it either way; this
   * only keeps a button off the screen for somebody who would be told no.
   */
  assistantEnabled: boolean;
  /**
   * When this account's paid access runs out, absent for an account that has never subscribed -
   * which is every account until a checkout writes one. A message field, so proto3 gives it
   * presence without the `optional` keyword.
   */
  subscribedUntil?: string | undefined;
}

/**
 * UpdateCurrentUserRequest is the body of `PUT /users/me` - the client-settable half of a profile.
 * The id comes from the verified token and the timestamps from the server, so neither is here.
 */
export interface UpdateCurrentUserRequest {
  /**
   * Optional so an omitted username is distinguishable from one explicitly set to "": the first
   * keeps the name the profile holds, the second is a name SetUsername rejects. Without presence
   * the two would be one value, and a cleared field would get a silent success.
   */
  username?: string | undefined;
  bio: string;
}

function createBaseUser(): User {
  return { id: "", username: "", bio: "", createdAt: undefined, updatedAt: undefined };
}

export const User: MessageFns<User> = {
  encode(message: User, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.bio !== "") {
      writer.uint32(26).string(message.bio);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(34).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): User {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUser();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.id = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.username = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.bio = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): User {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      bio: isSet(object.bio) ? globalThis.String(object.bio) : "",
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: User): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.bio !== "") {
      obj.bio = message.bio;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<User>, I>>(base?: I): User {
    return User.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<User>, I>>(object: I): User {
    const message = createBaseUser();
    message.id = object.id ?? "";
    message.username = object.username ?? "";
    message.bio = object.bio ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseCurrentUser(): CurrentUser {
  return {
    id: "",
    username: "",
    bio: "",
    createdAt: undefined,
    updatedAt: undefined,
    assistantEnabled: false,
    subscribedUntil: undefined,
  };
}

export const CurrentUser: MessageFns<CurrentUser> = {
  encode(message: CurrentUser, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.bio !== "") {
      writer.uint32(26).string(message.bio);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(34).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(42).fork()).join();
    }
    if (message.assistantEnabled !== false) {
      writer.uint32(48).bool(message.assistantEnabled);
    }
    if (message.subscribedUntil !== undefined) {
      Timestamp.encode(toTimestamp(message.subscribedUntil), writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CurrentUser {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCurrentUser();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.id = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.username = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.bio = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.assistantEnabled = reader.bool();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.subscribedUntil = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CurrentUser {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      bio: isSet(object.bio) ? globalThis.String(object.bio) : "",
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : undefined,
      assistantEnabled: isSet(object.assistantEnabled)
        ? globalThis.Boolean(object.assistantEnabled)
        : isSet(object.assistant_enabled)
        ? globalThis.Boolean(object.assistant_enabled)
        : false,
      subscribedUntil: isSet(object.subscribedUntil)
        ? globalThis.String(object.subscribedUntil)
        : isSet(object.subscribed_until)
        ? globalThis.String(object.subscribed_until)
        : undefined,
    };
  },

  toJSON(message: CurrentUser): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.bio !== "") {
      obj.bio = message.bio;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt;
    }
    if (message.assistantEnabled !== false) {
      obj.assistantEnabled = message.assistantEnabled;
    }
    if (message.subscribedUntil !== undefined) {
      obj.subscribedUntil = message.subscribedUntil;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CurrentUser>, I>>(base?: I): CurrentUser {
    return CurrentUser.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CurrentUser>, I>>(object: I): CurrentUser {
    const message = createBaseCurrentUser();
    message.id = object.id ?? "";
    message.username = object.username ?? "";
    message.bio = object.bio ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.assistantEnabled = object.assistantEnabled ?? false;
    message.subscribedUntil = object.subscribedUntil ?? undefined;
    return message;
  },
};

function createBaseUpdateCurrentUserRequest(): UpdateCurrentUserRequest {
  return { username: undefined, bio: "" };
}

export const UpdateCurrentUserRequest: MessageFns<UpdateCurrentUserRequest> = {
  encode(message: UpdateCurrentUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== undefined) {
      writer.uint32(10).string(message.username);
    }
    if (message.bio !== "") {
      writer.uint32(18).string(message.bio);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateCurrentUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUpdateCurrentUserRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.username = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.bio = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UpdateCurrentUserRequest {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : undefined,
      bio: isSet(object.bio) ? globalThis.String(object.bio) : "",
    };
  },

  toJSON(message: UpdateCurrentUserRequest): unknown {
    const obj: any = {};
    if (message.username !== undefined) {
      obj.username = message.username;
    }
    if (message.bio !== "") {
      obj.bio = message.bio;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateCurrentUserRequest>, I>>(base?: I): UpdateCurrentUserRequest {
    return UpdateCurrentUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateCurrentUserRequest>, I>>(object: I): UpdateCurrentUserRequest {
    const message = createBaseUpdateCurrentUserRequest();
    message.username = object.username ?? undefined;
    message.bio = object.bio ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
