// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.4
//   protoc               unknown
// source: blog/v1/reaction.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "blog.v1";

/**
 * ReactionCount is one emoji as a bar renders it: the glyph, how many readers chose it, and
 * whether the caller is one of them.
 *
 * It has no entity counterpart at all, deliberately - it is not entity.Reaction with its fields
 * renamed. What crosses the wire is an aggregate folded from every reader's stored row
 * (internal/service/reaction.go's countReactions), not a stored row itself: entity.Reaction is one
 * reader's choices, keyed by reader, while this is one emoji's count across every reader. Who
 * reacted is deliberately not reported here either - a count and a "you are in it" is the whole of
 * what a bar draws, and naming the readers would turn a one-click gesture into a public record of
 * who liked what.
 */
export interface ReactionCount {
  emoji: string;
  count: number;
  reacted: boolean;
}

/**
 * TargetReactions is every count on one target - the post, or one of its comments - in the bar's
 * own order (most chosen first). It is what `PUT`/`DELETE .../reactions/{emoji}` answer with, and
 * what each entry of PageReactions.comments below holds; a bare `repeated ReactionCount` cannot be
 * a map value or a top-level protojson body on its own, so this is the message that lets it be
 * both. Like ReactionCount it has no entity counterpart - a target's counts are folded fresh from
 * every reaction on it, never stored this way.
 *
 * There is deliberately no PutReactionRequest/DeleteReactionRequest beside it: a reaction is
 * addressed, not toggled - the target and the emoji are both in the URL
 * (`.../reactions/{emoji}`), and the reader is the credential - so PutReaction and DeleteReaction
 * read no request body at all.
 */
export interface TargetReactions {
  reactions: ReactionCount[];
}

/**
 * PageReactions is every reaction on a post page, in one response: the post's own, and each
 * reacted-to comment's by id. It is one message because it answers one query - a comment's
 * reactions are stored beneath the post alongside the post's own (see `services/backend/AGENTS.md`'s "Reader
 * Engagement" section), so splitting them across the responses that carry a post and its comments
 * would mean reading them twice to say the same thing. Like ReactionCount and TargetReactions, it
 * has no entity counterpart - it exists only as a wire shape.
 *
 * A comment with no reactions is absent from `comments` rather than present and empty, so the map
 * holds only what there is something to draw for.
 */
export interface PageReactions {
  post: ReactionCount[];
  comments: { [key: string]: TargetReactions };
}

export interface PageReactions_CommentsEntry {
  key: string;
  value?: TargetReactions | undefined;
}

function createBaseReactionCount(): ReactionCount {
  return { emoji: "", count: 0, reacted: false };
}

export const ReactionCount: MessageFns<ReactionCount> = {
  encode(message: ReactionCount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.emoji !== "") {
      writer.uint32(10).string(message.emoji);
    }
    if (message.count !== 0) {
      writer.uint32(16).int32(message.count);
    }
    if (message.reacted !== false) {
      writer.uint32(24).bool(message.reacted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReactionCount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseReactionCount();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.emoji = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.count = reader.int32();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.reacted = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ReactionCount {
    return {
      emoji: isSet(object.emoji) ? globalThis.String(object.emoji) : "",
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      reacted: isSet(object.reacted) ? globalThis.Boolean(object.reacted) : false,
    };
  },

  toJSON(message: ReactionCount): unknown {
    const obj: any = {};
    if (message.emoji !== "") {
      obj.emoji = message.emoji;
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.reacted !== false) {
      obj.reacted = message.reacted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReactionCount>, I>>(base?: I): ReactionCount {
    return ReactionCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReactionCount>, I>>(object: I): ReactionCount {
    const message = createBaseReactionCount();
    message.emoji = object.emoji ?? "";
    message.count = object.count ?? 0;
    message.reacted = object.reacted ?? false;
    return message;
  },
};

function createBaseTargetReactions(): TargetReactions {
  return { reactions: [] };
}

export const TargetReactions: MessageFns<TargetReactions> = {
  encode(message: TargetReactions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.reactions) {
      ReactionCount.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TargetReactions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseTargetReactions();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.reactions.push(ReactionCount.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): TargetReactions {
    return {
      reactions: globalThis.Array.isArray(object?.reactions)
        ? object.reactions.map((e: any) => ReactionCount.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TargetReactions): unknown {
    const obj: any = {};
    if (message.reactions?.length) {
      obj.reactions = message.reactions.map((e) => ReactionCount.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TargetReactions>, I>>(base?: I): TargetReactions {
    return TargetReactions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TargetReactions>, I>>(object: I): TargetReactions {
    const message = createBaseTargetReactions();
    message.reactions = object.reactions?.map((e) => ReactionCount.fromPartial(e)) || [];
    return message;
  },
};

function createBasePageReactions(): PageReactions {
  return { post: [], comments: {} };
}

export const PageReactions: MessageFns<PageReactions> = {
  encode(message: PageReactions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.post) {
      ReactionCount.encode(v!, writer.uint32(10).fork()).join();
    }
    globalThis.Object.entries(message.comments).forEach(([key, value]: [string, TargetReactions]) => {
      PageReactions_CommentsEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PageReactions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePageReactions();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.post.push(ReactionCount.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            const entry2 = PageReactions_CommentsEntry.decode(reader, reader.uint32());
            if (entry2.value !== undefined) {
              message.comments[entry2.key] = entry2.value;
            }
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): PageReactions {
    return {
      post: globalThis.Array.isArray(object?.post) ? object.post.map((e: any) => ReactionCount.fromJSON(e)) : [],
      comments: isObject(object.comments)
        ? (globalThis.Object.entries(object.comments) as [string, any][]).reduce(
          (acc: { [key: string]: TargetReactions }, [key, value]: [string, any]) => {
            globalThis.Object.defineProperty(acc, key, {
              value: TargetReactions.fromJSON(value),
              enumerable: true,
              configurable: true,
              writable: true,
            });
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: PageReactions): unknown {
    const obj: any = {};
    if (message.post?.length) {
      obj.post = message.post.map((e) => ReactionCount.toJSON(e));
    }
    if (message.comments) {
      const entries = globalThis.Object.entries(message.comments) as [string, TargetReactions][];
      if (entries.length > 0) {
        obj.comments = {};
        entries.forEach(([k, v]) => {
          obj.comments[k] = TargetReactions.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PageReactions>, I>>(base?: I): PageReactions {
    return PageReactions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PageReactions>, I>>(object: I): PageReactions {
    const message = createBasePageReactions();
    message.post = object.post?.map((e) => ReactionCount.fromPartial(e)) || [];
    message.comments = (globalThis.Object.entries(object.comments ?? {}) as [string, TargetReactions][]).reduce(
      (acc: { [key: string]: TargetReactions }, [key, value]: [string, TargetReactions]) => {
        if (value !== undefined) {
          acc[key] = TargetReactions.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBasePageReactions_CommentsEntry(): PageReactions_CommentsEntry {
  return { key: "", value: undefined };
}

export const PageReactions_CommentsEntry: MessageFns<PageReactions_CommentsEntry> = {
  encode(message: PageReactions_CommentsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      TargetReactions.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PageReactions_CommentsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePageReactions_CommentsEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = TargetReactions.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): PageReactions_CommentsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? TargetReactions.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: PageReactions_CommentsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = TargetReactions.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PageReactions_CommentsEntry>, I>>(base?: I): PageReactions_CommentsEntry {
    return PageReactions_CommentsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PageReactions_CommentsEntry>, I>>(object: I): PageReactions_CommentsEntry {
    const message = createBasePageReactions_CommentsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? TargetReactions.fromPartial(object.value)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
