// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.4
//   protoc               unknown
// source: blog/v1/blog.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "blog.v1";

/**
 * Blog is a post as a client sees it - what `GET /blogs/{slug}` and every other read of a single
 * post answer with, and what each entry of BlogPage.posts is.
 *
 * It is the wire shape, not entity.Blog: author_username is resolved server-side
 * (internal/service/blog.go's blogResponse) and has no counterpart on the entity, while the
 * entity's DeletedAt - a soft-delete marker no response has ever included - has no field here at
 * all. See `packages/protos/CLAUDE.md`'s "Contract Layer" for why protos model the wire rather than the domain.
 */
export interface Blog {
  /**
   * The whole of the post's address, assigned once at creation and never revised - a retitle
   * changes `title` beneath a fixed slug rather than moving the URL. Slugs are unique across every
   * author, so this alone identifies a post; it carries no author segment.
   */
  slug: string;
  ownerId: string;
  /**
   * The owner's username, resolved server-side rather than stored on the post: a post records its
   * owner by uid, which is never public, so this is the only handle a client holds for the author
   * behind it. Empty for a post whose owner holds no profile.
   */
  authorUsername: string;
  title: string;
  content: string;
  /**
   * The topics a post is filed under, normalized server-side (lowercase, one hyphen between
   * words). They narrow a feed and say nothing about who may read it, which is `visibility`'s
   * alone.
   */
  tags: string[];
  /**
   * "public" or "private" - see the "Contract Layer" section of `packages/protos/CLAUDE.md` for why this is a plain
   * string rather than a proto enum.
   */
  visibility: string;
  allowedUserIds: string[];
  createdAt?: string | undefined;
  updatedAt?: string | undefined;
}

/**
 * BlogPage is one page of `GET /blogs`: the posts themselves, newest first, plus whether a further
 * page follows. There is no separate cursor field - a caller continues by sending the createdAt on
 * the last post here back as ListBlogsParams.start_after.
 */
export interface BlogPage {
  posts: Blog[];
  /**
   * Whether a further page follows. A plain bool rather than optional, so a page that happens to
   * be the last one still carries `hasMore: false` on the wire rather than omitting the field -
   * exactly what EmitDefaultValues (see internal/service/respond.go) guarantees for a scalar.
   */
  hasMore: boolean;
}

/**
 * ListBlogsParams is what `GET /blogs` pages and scopes by, all optional so the bare call still
 * means "the feed".
 *
 * Unlike every other message here, this one never crosses the wire as JSON: `GET /blogs` reads a
 * query string, not a body, so the backend keeps parsing it by hand (see
 * internal/service/blog.go's parseBlogListParams) and this message exists only to give the
 * frontend one generated type for the params it sends, rather than a second hand-written
 * interface that could drift from the query keys the backend actually reads.
 */
export interface ListBlogsParams {
  /**
   * How many posts a page holds. The backend applies its own default and cap when omitted -
   * explicit presence (rather than treating 0 as "omitted") is what lets that distinction survive
   * the trip through a generated type at all.
   */
  limit?:
    | number
    | undefined;
  /** Continues a previous page: the createdAt of the last post the previous one held. */
  startAfter?:
    | string
    | undefined;
  /** Narrows to one author's posts - a profile feed's User.id, not their username. */
  ownerId?:
    | string
    | undefined;
  /** Narrows to one topic. Any spelling works; the backend normalizes it before matching. */
  tag?:
    | string
    | undefined;
  /**
   * Narrows to posts holding this term in their title or body, ignoring case. Never widens what
   * comes back: a post the caller may not read stays hidden however exactly it is named.
   */
  q?: string | undefined;
}

/**
 * BlogRequest is the client-settable half of a post - the body of both `POST /blogs` and
 * `PUT /blogs/{slug}`, which share the same shape (internal/service/blog.go's blogRequest and
 * decodeBlogRequest serve both routes with it). The slug, owner_id, and the timestamps are decided
 * by the server, so none of them are here.
 */
export interface BlogRequest {
  title: string;
  content: string;
  tags: string[];
  /**
   * "public" or "private" - see the "Contract Layer" section of `packages/protos/CLAUDE.md` for why this is a plain
   * string rather than a proto enum.
   */
  visibility: string;
  allowedUserIds: string[];
}

function createBaseBlog(): Blog {
  return {
    slug: "",
    ownerId: "",
    authorUsername: "",
    title: "",
    content: "",
    tags: [],
    visibility: "",
    allowedUserIds: [],
    createdAt: undefined,
    updatedAt: undefined,
  };
}

export const Blog: MessageFns<Blog> = {
  encode(message: Blog, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slug !== "") {
      writer.uint32(10).string(message.slug);
    }
    if (message.ownerId !== "") {
      writer.uint32(18).string(message.ownerId);
    }
    if (message.authorUsername !== "") {
      writer.uint32(26).string(message.authorUsername);
    }
    if (message.title !== "") {
      writer.uint32(34).string(message.title);
    }
    if (message.content !== "") {
      writer.uint32(42).string(message.content);
    }
    for (const v of message.tags) {
      writer.uint32(50).string(v!);
    }
    if (message.visibility !== "") {
      writer.uint32(58).string(message.visibility);
    }
    for (const v of message.allowedUserIds) {
      writer.uint32(66).string(v!);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(74).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Blog {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBlog();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.slug = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.ownerId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.authorUsername = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.title = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.content = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.tags.push(reader.string());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.visibility = reader.string();
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.allowedUserIds.push(reader.string());
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Blog {
    return {
      slug: isSet(object.slug) ? globalThis.String(object.slug) : "",
      ownerId: isSet(object.ownerId)
        ? globalThis.String(object.ownerId)
        : isSet(object.owner_id)
        ? globalThis.String(object.owner_id)
        : "",
      authorUsername: isSet(object.authorUsername)
        ? globalThis.String(object.authorUsername)
        : isSet(object.author_username)
        ? globalThis.String(object.author_username)
        : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => globalThis.String(e)) : [],
      visibility: isSet(object.visibility) ? globalThis.String(object.visibility) : "",
      allowedUserIds: globalThis.Array.isArray(object?.allowedUserIds)
        ? object.allowedUserIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.allowed_user_ids)
        ? object.allowed_user_ids.map((e: any) => globalThis.String(e))
        : [],
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: Blog): unknown {
    const obj: any = {};
    if (message.slug !== "") {
      obj.slug = message.slug;
    }
    if (message.ownerId !== "") {
      obj.ownerId = message.ownerId;
    }
    if (message.authorUsername !== "") {
      obj.authorUsername = message.authorUsername;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    if (message.visibility !== "") {
      obj.visibility = message.visibility;
    }
    if (message.allowedUserIds?.length) {
      obj.allowedUserIds = message.allowedUserIds;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Blog>, I>>(base?: I): Blog {
    return Blog.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Blog>, I>>(object: I): Blog {
    const message = createBaseBlog();
    message.slug = object.slug ?? "";
    message.ownerId = object.ownerId ?? "";
    message.authorUsername = object.authorUsername ?? "";
    message.title = object.title ?? "";
    message.content = object.content ?? "";
    message.tags = object.tags?.map((e) => e) || [];
    message.visibility = object.visibility ?? "";
    message.allowedUserIds = object.allowedUserIds?.map((e) => e) || [];
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseBlogPage(): BlogPage {
  return { posts: [], hasMore: false };
}

export const BlogPage: MessageFns<BlogPage> = {
  encode(message: BlogPage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.posts) {
      Blog.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.hasMore !== false) {
      writer.uint32(16).bool(message.hasMore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BlogPage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBlogPage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.posts.push(Blog.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.hasMore = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): BlogPage {
    return {
      posts: globalThis.Array.isArray(object?.posts) ? object.posts.map((e: any) => Blog.fromJSON(e)) : [],
      hasMore: isSet(object.hasMore)
        ? globalThis.Boolean(object.hasMore)
        : isSet(object.has_more)
        ? globalThis.Boolean(object.has_more)
        : false,
    };
  },

  toJSON(message: BlogPage): unknown {
    const obj: any = {};
    if (message.posts?.length) {
      obj.posts = message.posts.map((e) => Blog.toJSON(e));
    }
    if (message.hasMore !== false) {
      obj.hasMore = message.hasMore;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BlogPage>, I>>(base?: I): BlogPage {
    return BlogPage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BlogPage>, I>>(object: I): BlogPage {
    const message = createBaseBlogPage();
    message.posts = object.posts?.map((e) => Blog.fromPartial(e)) || [];
    message.hasMore = object.hasMore ?? false;
    return message;
  },
};

function createBaseListBlogsParams(): ListBlogsParams {
  return { limit: undefined, startAfter: undefined, ownerId: undefined, tag: undefined, q: undefined };
}

export const ListBlogsParams: MessageFns<ListBlogsParams> = {
  encode(message: ListBlogsParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.limit !== undefined) {
      writer.uint32(8).int32(message.limit);
    }
    if (message.startAfter !== undefined) {
      writer.uint32(18).string(message.startAfter);
    }
    if (message.ownerId !== undefined) {
      writer.uint32(26).string(message.ownerId);
    }
    if (message.tag !== undefined) {
      writer.uint32(34).string(message.tag);
    }
    if (message.q !== undefined) {
      writer.uint32(42).string(message.q);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListBlogsParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseListBlogsParams();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.limit = reader.int32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.startAfter = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.ownerId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.tag = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.q = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ListBlogsParams {
    return {
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : undefined,
      startAfter: isSet(object.startAfter)
        ? globalThis.String(object.startAfter)
        : isSet(object.start_after)
        ? globalThis.String(object.start_after)
        : undefined,
      ownerId: isSet(object.ownerId)
        ? globalThis.String(object.ownerId)
        : isSet(object.owner_id)
        ? globalThis.String(object.owner_id)
        : undefined,
      tag: isSet(object.tag) ? globalThis.String(object.tag) : undefined,
      q: isSet(object.q) ? globalThis.String(object.q) : undefined,
    };
  },

  toJSON(message: ListBlogsParams): unknown {
    const obj: any = {};
    if (message.limit !== undefined) {
      obj.limit = Math.round(message.limit);
    }
    if (message.startAfter !== undefined) {
      obj.startAfter = message.startAfter;
    }
    if (message.ownerId !== undefined) {
      obj.ownerId = message.ownerId;
    }
    if (message.tag !== undefined) {
      obj.tag = message.tag;
    }
    if (message.q !== undefined) {
      obj.q = message.q;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListBlogsParams>, I>>(base?: I): ListBlogsParams {
    return ListBlogsParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListBlogsParams>, I>>(object: I): ListBlogsParams {
    const message = createBaseListBlogsParams();
    message.limit = object.limit ?? undefined;
    message.startAfter = object.startAfter ?? undefined;
    message.ownerId = object.ownerId ?? undefined;
    message.tag = object.tag ?? undefined;
    message.q = object.q ?? undefined;
    return message;
  },
};

function createBaseBlogRequest(): BlogRequest {
  return { title: "", content: "", tags: [], visibility: "", allowedUserIds: [] };
}

export const BlogRequest: MessageFns<BlogRequest> = {
  encode(message: BlogRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    for (const v of message.tags) {
      writer.uint32(26).string(v!);
    }
    if (message.visibility !== "") {
      writer.uint32(34).string(message.visibility);
    }
    for (const v of message.allowedUserIds) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BlogRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBlogRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.title = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.content = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.tags.push(reader.string());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.visibility = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.allowedUserIds.push(reader.string());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): BlogRequest {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => globalThis.String(e)) : [],
      visibility: isSet(object.visibility) ? globalThis.String(object.visibility) : "",
      allowedUserIds: globalThis.Array.isArray(object?.allowedUserIds)
        ? object.allowedUserIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.allowed_user_ids)
        ? object.allowed_user_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: BlogRequest): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    if (message.visibility !== "") {
      obj.visibility = message.visibility;
    }
    if (message.allowedUserIds?.length) {
      obj.allowedUserIds = message.allowedUserIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BlogRequest>, I>>(base?: I): BlogRequest {
    return BlogRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BlogRequest>, I>>(object: I): BlogRequest {
    const message = createBaseBlogRequest();
    message.title = object.title ?? "";
    message.content = object.content ?? "";
    message.tags = object.tags?.map((e) => e) || [];
    message.visibility = object.visibility ?? "";
    message.allowedUserIds = object.allowedUserIds?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
