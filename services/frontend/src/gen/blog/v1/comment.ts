// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.4
//   protoc               unknown
// source: blog/v1/comment.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "blog.v1";

/**
 * Comment is one reader's reply to a post, as clients see it - what `GET /blogs/{slug}/comments`
 * and `POST /blogs/{slug}/comments` answer with.
 *
 * It is the wire shape, not entity.Comment: author_username is resolved server-side
 * (internal/service/comment.go's withCommentAuthors) and has no counterpart on the entity, the
 * same pattern blog.proto's Blog follows for author_username there. entity.Comment's ownership and
 * moderation rules stay hand-written policy - see `packages/protos/CLAUDE.md`'s "Contract Layer" - so this message
 * carries no method of its own, only the fields a client reads.
 *
 * A comment is never edited, only written and removed, so there is no updated_at field and no
 * update route to answer with one.
 */
export interface Comment {
  /**
   * Assigned by the repository that stores it, and meaningful only beneath the post it hangs off -
   * see entity.Comment's own comment on why it has no name of its own to be addressed by.
   */
  id: string;
  blogSlug: string;
  /**
   * The uid of whoever wrote it, which a client needs to know whether to offer a delete button -
   * never a public identifier on its own, unlike author_username below.
   */
  authorId: string;
  /**
   * The commenter's username, resolved server-side rather than stored on the comment: it records
   * its author by uid, which is never public, so this is the only handle a client holds for the
   * profile behind it. Empty for a commenter who holds no profile.
   */
  authorUsername: string;
  body: string;
  createdAt?: string | undefined;
}

/**
 * CreateCommentRequest is the body of `POST /blogs/{slug}/comments` - the client-settable half of a
 * comment. The post it is on comes from the URL, its author from the credential, and its id and
 * timestamp from the server, so none of them are here.
 */
export interface CreateCommentRequest {
  body: string;
}

/**
 * CommentThread is the whole thread beneath a post, oldest first - what `GET
 * /blogs/{slug}/comments` answers with. It exists purely because protojson can only marshal a
 * message at the top level, never a bare array (the same reason reaction.proto's TargetReactions
 * wraps a `repeated ReactionCount`); the wire shape a client actually reads is still a plain list,
 * now carried in `comments` instead of being the whole body.
 */
export interface CommentThread {
  comments: Comment[];
}

function createBaseComment(): Comment {
  return { id: "", blogSlug: "", authorId: "", authorUsername: "", body: "", createdAt: undefined };
}

export const Comment: MessageFns<Comment> = {
  encode(message: Comment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.blogSlug !== "") {
      writer.uint32(18).string(message.blogSlug);
    }
    if (message.authorId !== "") {
      writer.uint32(26).string(message.authorId);
    }
    if (message.authorUsername !== "") {
      writer.uint32(34).string(message.authorUsername);
    }
    if (message.body !== "") {
      writer.uint32(42).string(message.body);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Comment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseComment();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.id = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.blogSlug = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.authorId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.authorUsername = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.body = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Comment {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      blogSlug: isSet(object.blogSlug)
        ? globalThis.String(object.blogSlug)
        : isSet(object.blog_slug)
        ? globalThis.String(object.blog_slug)
        : "",
      authorId: isSet(object.authorId)
        ? globalThis.String(object.authorId)
        : isSet(object.author_id)
        ? globalThis.String(object.author_id)
        : "",
      authorUsername: isSet(object.authorUsername)
        ? globalThis.String(object.authorUsername)
        : isSet(object.author_username)
        ? globalThis.String(object.author_username)
        : "",
      body: isSet(object.body) ? globalThis.String(object.body) : "",
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : undefined,
    };
  },

  toJSON(message: Comment): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.blogSlug !== "") {
      obj.blogSlug = message.blogSlug;
    }
    if (message.authorId !== "") {
      obj.authorId = message.authorId;
    }
    if (message.authorUsername !== "") {
      obj.authorUsername = message.authorUsername;
    }
    if (message.body !== "") {
      obj.body = message.body;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Comment>, I>>(base?: I): Comment {
    return Comment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Comment>, I>>(object: I): Comment {
    const message = createBaseComment();
    message.id = object.id ?? "";
    message.blogSlug = object.blogSlug ?? "";
    message.authorId = object.authorId ?? "";
    message.authorUsername = object.authorUsername ?? "";
    message.body = object.body ?? "";
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseCreateCommentRequest(): CreateCommentRequest {
  return { body: "" };
}

export const CreateCommentRequest: MessageFns<CreateCommentRequest> = {
  encode(message: CreateCommentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.body !== "") {
      writer.uint32(10).string(message.body);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateCommentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCreateCommentRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.body = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CreateCommentRequest {
    return { body: isSet(object.body) ? globalThis.String(object.body) : "" };
  },

  toJSON(message: CreateCommentRequest): unknown {
    const obj: any = {};
    if (message.body !== "") {
      obj.body = message.body;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateCommentRequest>, I>>(base?: I): CreateCommentRequest {
    return CreateCommentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateCommentRequest>, I>>(object: I): CreateCommentRequest {
    const message = createBaseCreateCommentRequest();
    message.body = object.body ?? "";
    return message;
  },
};

function createBaseCommentThread(): CommentThread {
  return { comments: [] };
}

export const CommentThread: MessageFns<CommentThread> = {
  encode(message: CommentThread, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.comments) {
      Comment.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommentThread {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCommentThread();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.comments.push(Comment.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CommentThread {
    return {
      comments: globalThis.Array.isArray(object?.comments) ? object.comments.map((e: any) => Comment.fromJSON(e)) : [],
    };
  },

  toJSON(message: CommentThread): unknown {
    const obj: any = {};
    if (message.comments?.length) {
      obj.comments = message.comments.map((e) => Comment.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommentThread>, I>>(base?: I): CommentThread {
    return CommentThread.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommentThread>, I>>(object: I): CommentThread {
    const message = createBaseCommentThread();
    message.comments = object.comments?.map((e) => Comment.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
