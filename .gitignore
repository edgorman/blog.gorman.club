# Pants build metadata, packaged output, and pantsd pid files.
.pants.d/
dist/
.pids/

# moon's local cache and downloaded schemas.
.moon/cache

# Claude Code personal settings overrides - never committed.
.claude/settings.local.json
