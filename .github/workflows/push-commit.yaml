name: 'Push Commit'

on:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: push-commit-root-main
  cancel-in-progress: false

jobs:
  diff:
    runs-on: ubuntu-latest
    outputs:
      infrastructure-root: ${{ steps.diff.outputs.infrastructure-root }}
      services-frontend: ${{ steps.diff.outputs.services-frontend }}
    steps:
      - uses: actions/checkout@v5
      - id: diff
        uses: ./.github/actions/github-file-diff
        with:
          branch_ref: ${{ github.ref_name }}

  # Deploys the frontend straight to Cloudflare Pages on every merge to
  # main, per the Staging Deployments section of CLAUDE.md. Cloudflare has
  # no WIF-style OIDC federation like the GCP jobs below, so its credentials
  # are plain GitHub Actions secrets/variables instead of Secret Manager:
  # CLOUDFLARE_API_TOKEN (secret) and CLOUDFLARE_ACCOUNT_ID (variable) need
  # to be created by hand once, the same way root's bootstrap PAT was.
  # backend_url is left unset until the backend service exists; until then
  # the deployed site shows the "no backend deployed yet" placeholder.
  services-frontend:
    needs: diff
    if: needs.diff.outputs.services-frontend == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/frontend-push-commit
        with:
          cloudflare_api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare_account_id: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare_project_name: 'frontend-stag'

  infrastructure-root:
    needs: diff
    if: needs.diff.outputs.infrastructure-root == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v5
      - id: secrets
        uses: ./.github/actions/gcp-secret-manager
        with:
          project_id: 'blog-gorman-club-root'
          workload_identity_provider: ${{ vars.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.SERVICE_ACCOUNT }}
          secrets_list: |
            TF_VAR_github_provider_token=github_provider_token
            TF_VAR_cloudflare_account_id=cloudflare_account_id
            TF_VAR_cloudflare_api_token=cloudflare_api_token
      - uses: ./.github/actions/terraform-push-commit
        with:
          terraform_directory: 'infrastructure/root'
          config_directory: 'infrastructure/config/root'
          secrets: ${{ steps.secrets.outputs.secrets }}

  # Runs on every merge to main, independent of the diff job above: the
  # default patch-bump pre-release is unconditional per CLAUDE.md, not
  # gated on which part of the repo changed.
  pre-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - id: version
        name: Calculate Next Version
        shell: bash
        run: |
          LAST_TAG=$(git tag --list 'v*' --sort=-v:refname | head -n1)
          LAST_TAG=${LAST_TAG:-v0.0.0}
          IFS='.' read -r MAJOR MINOR PATCH <<< "${LAST_TAG#v}"
          {
            echo "version=v${MAJOR}.${MINOR}.$((PATCH + 1))"
            echo "last_tag=$LAST_TAG"
          } >> "$GITHUB_OUTPUT"

      - id: commits
        name: Summarize Commits Since Last Release
        shell: bash
        run: |
          if git rev-parse "${{ steps.version.outputs.last_tag }}" >/dev/null 2>&1; then
            RANGE="${{ steps.version.outputs.last_tag }}..HEAD"
          else
            RANGE="HEAD"
          fi
          {
            echo 'log<<COMMITS_EOF'
            git log "$RANGE" --pretty='- %s (%h)'
            echo 'COMMITS_EOF'
          } >> "$GITHUB_OUTPUT"

      # The prod terraform plan CLAUDE.md describes as part of this step is
      # deliberately left out until infrastructure/config/prod (the
      # env-level Terraform root) actually exists - same "wire it up once
      # that part of the repo exists" rule as the staging/prod filters
      # noted in github-file-diff.
      - name: Write Release Notes
        shell: bash
        run: |
          {
            echo "## Commits since ${{ steps.version.outputs.last_tag }}"
            echo "${{ steps.commits.outputs.log }}"
          } > RELEASE_NOTES.md

      - name: Create Pre-Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.version }}
          target_commitish: ${{ github.sha }}
          prerelease: true
          body_path: RELEASE_NOTES.md
