# Committed TypeScript bindings generated by ts-proto from packages/protos/blog/v1/emoji.proto -
# see the "Contract Layer" section of CLAUDE.md for why committed rather than Pants-ephemeral, and
# packages/protos/buf.gen.yaml for the generator invocation. Regenerated by `buf generate` from
# packages/protos, checked for drift in CI (protos-drift in pull-request.yaml); never hand-edited.
#
# The explicit dependency on packages/protos is what makes `pants dependents`/`--changed-since`
# resolve this package (and its Go counterpart) from a `.proto`-only edit - see #155. It's a plain
# BUILD-graph edge, not a codegen trigger: no TypeScript protobuf codegen backend is registered in
# pants.toml (there isn't one to enable - see CLAUDE.md's Contract Layer section), so nothing tries
# to generate TypeScript from this target.
typescript_sources(
    sources=["*.ts"],
    dependencies=["//packages/protos"],
)
