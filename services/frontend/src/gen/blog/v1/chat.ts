// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.4
//   protoc               unknown
// source: blog/v1/chat.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { Blog } from "./blog";

export const protobufPackage = "blog.v1";

/**
 * ChatEdit records one change the assistant made to the post, alongside what it said - the wire
 * counterpart of entity.ChatEdit. It is a description rather than a diff: the post itself is the
 * record of its own contents, and a stored/transmitted diff would only be a second copy to fall
 * out of date.
 */
export interface ChatEdit {
  /** The name of the tool the model called, e.g. "set_title". */
  tool: string;
  /** A one-line description of the change, written for the person reading the chat. */
  summary: string;
}

/**
 * ChatMessage is one turn of the conversation, as clients see it - what GetChat's history and
 * SendChatMessage's reply both carry.
 *
 * role is a plain string ("user" or "assistant"), not a proto enum, for the same reason
 * blog.proto's Blog.visibility is: protojson serializes an enum by its member name, which would
 * force an ALL_CAPS rewrite on the wire for a field entity.ChatRole already constrains server-side
 * (see CLAUDE.md's "Contract Layer"). entity.ChatMessage.Validate stays the one place that
 * enforces it.
 */
export interface ChatMessage {
  role: string;
  /**
   * Empty for an assistant turn that only edited and said nothing about it - see
   * entity.ChatMessage.Validate, which requires content or edits but not both.
   */
  content: string;
  /** Empty for a user message and for an assistant turn that only answered a question. */
  edits: ChatEdit[];
  createdAt?: string | undefined;
}

/**
 * ChatHistory is a whole conversation about a post - what GetChat answers with. It exists purely
 * because protojson can only marshal a message at the top level, never a bare array (the same
 * reason comment.proto's CommentThread and reaction.proto's TargetReactions each wrap a repeated
 * field); the wire shape a client actually reads is still a plain list, carried in `messages`.
 */
export interface ChatHistory {
  messages: ChatMessage[];
}

/**
 * ChatRequest is the body of `POST /blogs/{slug}/chat` - one message from the author, optionally
 * carrying the draft they are looking at.
 *
 * title and content are optional, not because the assistant can be asked without them, but because
 * omitting them and sending an explicit empty string are different requests: omitted means "use
 * the post as it was saved", while "" is an author who has emptied the field in the editor and
 * wants the assistant to see that. The editor is a form with unsaved changes in it - "tighten this
 * paragraph" has to mean the paragraph on screen, not the one last written to Firestore - which is
 * exactly what a message field's presence (rather than a scalar's) preserves on the wire; see
 * CLAUDE.md's note on useOptionals=messages for why that distinction survives at all.
 */
export interface ChatRequest {
  message: string;
  title?: string | undefined;
  content?: string | undefined;
}

/**
 * ChatReply is one exchange with the assistant: what was said on both sides, and the post as it
 * now stands. It is the response to `POST /blogs/{slug}/chat`.
 *
 * blog comes back whole rather than as a diff because the assistant edits the author's live draft
 * server-side: the editor has to replace what is in its fields with what was actually stored, or
 * the next save would write the pre-assistant text back over it. updated says whether that
 * happened, so an editor whose author is still typing is only interrupted when there is really
 * something to show them.
 */
export interface ChatReply {
  messages: ChatMessage[];
  blog?: Blog | undefined;
  updated: boolean;
}

function createBaseChatEdit(): ChatEdit {
  return { tool: "", summary: "" };
}

export const ChatEdit: MessageFns<ChatEdit> = {
  encode(message: ChatEdit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tool !== "") {
      writer.uint32(10).string(message.tool);
    }
    if (message.summary !== "") {
      writer.uint32(18).string(message.summary);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChatEdit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseChatEdit();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.tool = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.summary = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ChatEdit {
    return {
      tool: isSet(object.tool) ? globalThis.String(object.tool) : "",
      summary: isSet(object.summary) ? globalThis.String(object.summary) : "",
    };
  },

  toJSON(message: ChatEdit): unknown {
    const obj: any = {};
    if (message.tool !== "") {
      obj.tool = message.tool;
    }
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChatEdit>, I>>(base?: I): ChatEdit {
    return ChatEdit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChatEdit>, I>>(object: I): ChatEdit {
    const message = createBaseChatEdit();
    message.tool = object.tool ?? "";
    message.summary = object.summary ?? "";
    return message;
  },
};

function createBaseChatMessage(): ChatMessage {
  return { role: "", content: "", edits: [], createdAt: undefined };
}

export const ChatMessage: MessageFns<ChatMessage> = {
  encode(message: ChatMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.role !== "") {
      writer.uint32(10).string(message.role);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    for (const v of message.edits) {
      ChatEdit.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChatMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseChatMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.role = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.content = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.edits.push(ChatEdit.decode(reader, reader.uint32()));
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ChatMessage {
    return {
      role: isSet(object.role) ? globalThis.String(object.role) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      edits: globalThis.Array.isArray(object?.edits) ? object.edits.map((e: any) => ChatEdit.fromJSON(e)) : [],
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : undefined,
    };
  },

  toJSON(message: ChatMessage): unknown {
    const obj: any = {};
    if (message.role !== "") {
      obj.role = message.role;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.edits?.length) {
      obj.edits = message.edits.map((e) => ChatEdit.toJSON(e));
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChatMessage>, I>>(base?: I): ChatMessage {
    return ChatMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChatMessage>, I>>(object: I): ChatMessage {
    const message = createBaseChatMessage();
    message.role = object.role ?? "";
    message.content = object.content ?? "";
    message.edits = object.edits?.map((e) => ChatEdit.fromPartial(e)) || [];
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseChatHistory(): ChatHistory {
  return { messages: [] };
}

export const ChatHistory: MessageFns<ChatHistory> = {
  encode(message: ChatHistory, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messages) {
      ChatMessage.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChatHistory {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseChatHistory();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.messages.push(ChatMessage.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ChatHistory {
    return {
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => ChatMessage.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ChatHistory): unknown {
    const obj: any = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => ChatMessage.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChatHistory>, I>>(base?: I): ChatHistory {
    return ChatHistory.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChatHistory>, I>>(object: I): ChatHistory {
    const message = createBaseChatHistory();
    message.messages = object.messages?.map((e) => ChatMessage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseChatRequest(): ChatRequest {
  return { message: "", title: undefined, content: undefined };
}

export const ChatRequest: MessageFns<ChatRequest> = {
  encode(message: ChatRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    if (message.title !== undefined) {
      writer.uint32(18).string(message.title);
    }
    if (message.content !== undefined) {
      writer.uint32(26).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChatRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseChatRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.message = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.title = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.content = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ChatRequest {
    return {
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : undefined,
      content: isSet(object.content) ? globalThis.String(object.content) : undefined,
    };
  },

  toJSON(message: ChatRequest): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.title !== undefined) {
      obj.title = message.title;
    }
    if (message.content !== undefined) {
      obj.content = message.content;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChatRequest>, I>>(base?: I): ChatRequest {
    return ChatRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChatRequest>, I>>(object: I): ChatRequest {
    const message = createBaseChatRequest();
    message.message = object.message ?? "";
    message.title = object.title ?? undefined;
    message.content = object.content ?? undefined;
    return message;
  },
};

function createBaseChatReply(): ChatReply {
  return { messages: [], blog: undefined, updated: false };
}

export const ChatReply: MessageFns<ChatReply> = {
  encode(message: ChatReply, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messages) {
      ChatMessage.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.blog !== undefined) {
      Blog.encode(message.blog, writer.uint32(18).fork()).join();
    }
    if (message.updated !== false) {
      writer.uint32(24).bool(message.updated);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChatReply {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseChatReply();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.messages.push(ChatMessage.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.blog = Blog.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.updated = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ChatReply {
    return {
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => ChatMessage.fromJSON(e))
        : [],
      blog: isSet(object.blog) ? Blog.fromJSON(object.blog) : undefined,
      updated: isSet(object.updated) ? globalThis.Boolean(object.updated) : false,
    };
  },

  toJSON(message: ChatReply): unknown {
    const obj: any = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => ChatMessage.toJSON(e));
    }
    if (message.blog !== undefined) {
      obj.blog = Blog.toJSON(message.blog);
    }
    if (message.updated !== false) {
      obj.updated = message.updated;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChatReply>, I>>(base?: I): ChatReply {
    return ChatReply.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChatReply>, I>>(object: I): ChatReply {
    const message = createBaseChatReply();
    message.messages = object.messages?.map((e) => ChatMessage.fromPartial(e)) || [];
    message.blog = (object.blog !== undefined && object.blog !== null) ? Blog.fromPartial(object.blog) : undefined;
    message.updated = object.updated ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
