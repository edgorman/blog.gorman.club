# Committed TypeScript bindings generated by ts-proto from packages/protos/blog/v1/emoji.proto -
# see the "Contract Layer" section of CLAUDE.md for why committed rather than Pants-ephemeral, and
# packages/protos/buf.gen.yaml for the generator invocation. Regenerated by `buf generate` from
# packages/protos, checked for drift in CI (protos-drift in pull-request.yaml); never hand-edited.
typescript_sources(
    sources=["*.ts"],
)
